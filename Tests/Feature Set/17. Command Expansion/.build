# !command runs a shell command at parse time and pastes its stdout into the
# value. The command must be a single plain token (no arguments, dots or path
# separators), so we use hostname and verify at runtime against COMPUTERNAME.
Assembly    CmdExpansion
SourceFiles main

HostFromCmd !hostname
Defines     BUILD_HOST=$HostFromCmd
