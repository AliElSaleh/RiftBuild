# Block if/else plus the single-line if form (which must stay on one line).
Assembly    IfElse
SourceFiles main

if windows
{
    Compiler.Defines FROM_IF_BLOCK=1
}
else
{
    Compiler.Defines FROM_ELSE_BLOCK=1
}

if x64 Compiler.Defines INLINE_IF=1
